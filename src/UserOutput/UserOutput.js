import React from "react";

const useroutput = props => {
  return (
    <div>
      <p>this is a test</p>
      <p>this is another test for {props.username}</p>
    </div>
  );
};

export default useroutput;
